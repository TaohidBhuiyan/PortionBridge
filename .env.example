# Port Configuration for Docker Compose
# Copy this file to .env and modify the ports as needed

# Database port (default: 3306)
DB_PORT=3306

# Server port (default: 5000)
SERVER_PORT=5000

# Client port (default: 80)
CLIENT_PORT=80
